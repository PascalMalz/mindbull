name: flutter_local_notifications_linux
description: Dummy package to bypass unwanted Linux plugin resolution
version: 0.0.1

environment:
  sdk: ">=2.17.0 <4.0.0"

flutter:
  plugin:
    platforms:
      linux:
        dartPluginClass: DummyPlugin
